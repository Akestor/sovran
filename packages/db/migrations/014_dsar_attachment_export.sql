-- DSAR: attachment metadata for export (Art. 15/20)
-- Fields: id, filename, content_type, size_bytes, created_at, server_id, channel_id, message_id
-- Excluded: object_key (internal path), no permanent download URLs
SELECT 1;
